# Copier ce fichier en .env.local (jamais committé) pour le développement local.
# Ces mêmes clés doivent aussi être ajoutées dans Vercel → Project → Settings →
# Environment Variables pour la production.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
